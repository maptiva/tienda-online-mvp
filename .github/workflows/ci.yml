name: 🛡️ PR Quality Check

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  validate:
    name: 🔍 Validar Calidad y Seguridad
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Instalar dependencias
        run: npm ci

      - name: 🏗️ Compilar Proyecto (Build)
        run: npm run build

      - name: 🧪 Ejecutar Validación Completa
        # Este comando corre: Lint Migraciones, Auditoría RLS, Tests de Seguridad y Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npm run validate:all

      - name: 📝 Reportar Éxito
        if: success()
        run: echo "✅ Todos los controles de calidad han pasado exitosamente."
